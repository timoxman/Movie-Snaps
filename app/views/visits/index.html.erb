<h1> temporarily this is a seperate screen </h1>
Current Scenes
<% if @scenes.any? %>
  <% @scenes.each do |scene| %>
    movie poster, name, location
    <%= scene.id %>
    <% if current_user %>
      <%= link_to "Add a visit", new_location_scene_visit_path(scene.location_id, scene.id) %>
    <% end %>
  <% end %>
<% else %>
  <h1>No visits yet</h1>
<% end %>
<br>


Curent Visits
<% if @visits.any? %>
  <% @visits.each do |visit| %>
    <% location = Scene.where("scene.id = ?",visit.scene_id).location_id %>
    <%= link_to visit.description, location_scene_visit(location,visit.scene_id,visit) %>
  <% end %>
<% else %>
  <h1>No visits yet</h1>
<% end %>
<br>


<% if current_user %>
  If no scene exists want to create a movie, location and scene as well as a visit
  <%= link_to "Add a visit", new_location_scene_visit_path(0) %>
<% end %>
