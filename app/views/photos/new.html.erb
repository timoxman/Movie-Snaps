<%= simple_form_for [@location, @scene, @visit, @photo], html: {multipart: true} do |f| %>
  <%= f.input :caption, label: 'Add your caption' %>
  <%= f.input :image, label: 'Upload image' %>
  <%= f.button :submit %>
<% end %>